# Backlog entry NN: <title>

> ⛔ **WHY THIS FORM EXISTS.** *`ADR-TEMPLATE.md` was the only template in this repo.* **Its fields are
> reached every time they apply** — provenance 2 of 2, implied scope 1 of 1, conjoined mechanisms 1 of
> 1, measured from each field's own birth. ⚠ **The form did not fail. RULINGS STOPPED TRAVELLING
> EXCLUSIVELY THROUGH IT.**
>
> **The worked cost:** the ruling that retired `threat_creation_rate`'s real side (entry 93) never
> asked which OTHER cells its reasoning implied. `pressure_to_sack` selects on the identical
> `was_pressure` column and **kept its grade**, and the inconsistency was found **by a grep, two
> dispatches later**. ⛔ **Not because a field was ignored — because that ruling's vehicle was a
> backlog entry and a dispatch brief, and NEITHER HAD FIELDS.**
>
> ⚠ **Use this form for any entry that RULES something.** A pure measurement or observation does not
> need it — **say so explicitly rather than leaving the sections blank.**

## Implied scope — REQUIRED for a ruling

**Name the cells, checks, metrics or channels this reasoning APPLIES TO but which this entry does NOT
change.** One line each, marked `unruled`. If genuinely none, say `none` — **but look first, and say
how you looked.**

> ⚠ **Derive the set; do not recall it.** A grep is a derivation and names the pattern it searched.
> **A remembered list is the thing that missed `pressure_to_sack`.**

## Provenance of factual claims — REQUIRED

**Every figure names its ARM** — which corpus, which tree, which branch of a probe (`canonical
496-game`, `entry NN probe (N plays)`, `DEFAULT`, `oracle / not-implementable`). ⛔ **A bare number is
a defect in the entry.**

**Every claim names its STATUS** — `MEASURED`, `DERIVED`, or `ASSERTED`.

> ⛔ **WHY BOTH.** *A claim placed among measurements is read as one.* **ADR-033 introduced a 400-sack
> sample as *"the terminal status is ALWAYS one `sackWhenNoTarget` names, and BOTH VALUES occur"* and
> then listed THREE bullets.** The third — *"frequently `CLEAN`"* — described what the RULE DOES and
> was never counted. ⚠ **It propagated into an engine source comment, a Tier 1 `definition` string,
> and a dispatch brief that quoted the comment back as EVIDENCE, purely because of where it sat on the
> page.**

## Premise ledger — REQUIRED

**Every claim this entry INHERITED rather than computed, and its disposition:** `CONFIRMED`,
`REFUTED`, or `CARRIED AS TESTIMONY`.

> ⛔ **REPORT IT EITHER WAY.** ⚠ **A structural check that returns nothing has produced a RESULT.**
> Left unreported, the question reads as **never asked** — which is the state the sim/real
> comparability claim sat in for a phase.

## What remains unjustified — REQUIRED when the entry records a REFUSAL

**A lever refused. State what is STILL UNJUSTIFIED about the value you did not change.**

⛔ **A refusal reports REACH — *"this lever does not move the metric."*** ⚠ **It must ALSO report
whether the value was ever DERIVED.** **Those are different questions and this register has treated
the first as partial credit toward the second.**

> ⛔ **WHY THIS FIELD EXISTS.** *A sweep answers "does this value matter." A derivation answers "why
> this value." Inertness feels like permission to stop asking — at exactly the moment the answer is
> cheapest, because the ruling-search has just been run and the record is open.*
>
> **The worked cost:** backlog entry 81 swept `arrival.collapsingWithinSeconds` across `0.0-2.0` and
> found it **structurally incapable** of moving the rate. ⚠ **Its opening even said *"step one returned
> UNRULED… every prior hit was this cell being USED, never EXAMINED"* — the fact was recorded, IN
> PROSE, and was lost downstream anyway.**
>
> **Then ADR-058 made that cell THE SOLE SWITCH** deciding whether an INTERIOR won rep floors
> `COLLAPSING` or `PRESSURE`. ⛔ **The justification was suddenly load-bearing, and had never existed —
> and the archaeology to establish that took a dedicated dispatch (entry 111) months later.**
>
> ⚠ **A VALUE PROVEN INERT TODAY IS A VALUE WHOSE JUSTIFICATION IS NEEDED THE DAY SOMETHING ELSE MAKES
> IT LIVE.** ⛔ **Recording it in prose was not enough; a NAMED SECTION is what survives a reader who
> is scanning for the verdict.**
>
> **⇒ Free at refusal time — the ruling-search has already run — and expensive afterwards, which is
> the test every field in this register has had to pass.**

**One line per unjustified component.** Use the same vocabulary as the provenance archaeology:
✅ `DERIVED` *(cite it)* / ⚠ `RULED` *(cite where)* / ⛔ `NEITHER` *(say so plainly — do not invent one)*.

## Finding

What was measured or established, with the arm named.

## Disposition

What changes, what does not, and what is left `unruled` for the owner.
